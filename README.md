# Image-Recognition-System-
Image Recognition System is a web-based application that identifies objects in uploaded images using Python, TensorFlow/Keras, OpenCV, and Flask. The CNN model is trained on image datasets and predicts the object with confidence. Users can upload an image through the web app and get a real-time classification result.
